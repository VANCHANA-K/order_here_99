route:
  receiver: default-webhook
  group_wait: 10s
  group_interval: 30s
  repeat_interval: 4h

receivers:
  - name: default-webhook
    webhook_configs:
      - url: http://host.docker.internal:18080/alerts
        send_resolved: true
